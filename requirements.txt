# FailDir — pip requirements.
#
# PESQ IS NOT HERE ON PURPOSE. There is no pip wheel for `pesq` on Windows /
# Python 3.12; pip tries to compile it and fails without MSVC. Install it from
# conda-forge instead:
#
#     conda install -c conda-forge pesq
#
# torch must match your CUDA runtime. For CUDA 12.4:
#
#     pip install torch --index-url https://download.pytorch.org/whl/cu124
#
# Versions below are the ones this code was verified against, as lower bounds.

numpy>=1.26
pandas>=2.1
scipy>=1.11
soundfile>=0.12
torch>=2.2
pystoi>=0.4.1
PyYAML>=6.0

# Claim 3
lightgbm>=4.0
scikit-learn>=1.3

# WER stage
jiwer>=3.0
faster-whisper>=1.0
transformers>=4.40      # only for EnglishTextNormalizer

# tooling
pytest>=8.0
ipykernel>=6.29
