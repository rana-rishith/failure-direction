# Shared across every run. Never override framing per-model — equal framing is
# what makes the four baselines comparable.
seed: 1337
sample_rate: 16000
stft:
  n_fft: 512
  hop: 128
  window: hann
paths:
  manifest: data/manifests/inventory.csv
  splits: data/splits
  runs: runs
  results: results
